# Homework 08 - Exploratory Data Analysis

The executed notebook completes numeric and categorical profiles, three distribution views, two bivariate relationships, a time-series view with date-gap validation, a correlation heatmap, and a reusable `src/eda.py::eda_summary` call. The synthetic schema is documented and seeded for reproducibility.
