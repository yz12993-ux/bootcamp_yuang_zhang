# Homework 09 - Feature Engineering

The executed notebook creates three feature families tied to Stage 08 findings: `income_log`, a past-only seven-day transaction mean, and one-hot region columns. `src/features.py` contains reusable implementations, and `data/processed/featured_spend_data.csv` stores the resulting modeling table.
